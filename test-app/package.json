{
  "name": "gridmaster-react-test-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4174 --configLoader native",
    "build": "tsc && vite build --configLoader native",
    "build:pages": "tsc && vite build --configLoader native --base=/gridmaster/",
    "build:site": "tsc && vite build --configLoader native --base=/gridmaster/test-app-pages/ --outDir=../test-app-pages --emptyOutDir",
    "preview": "vite preview --host 0.0.0.0 --port 4174 --configLoader native"
  },
  "dependencies": {
    "gridmaster-react": "^0.1.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^6.0.2",
    "vite": "^8.0.3"
  }
}
